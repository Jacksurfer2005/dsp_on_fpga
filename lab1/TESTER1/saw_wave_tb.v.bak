module saw_wave_tb(
);

 reg clk,rst;
 wire [7:0] out_signal;
 
 saw_wave dut(clk,rst,out_signal);
 
 initial
  begin
    clk = 0;
	 rst = 1;
  end
  
  always #5 clk = ~clk;
  
  initial 
   begin 
	  rst = 0;
	  #10;
	  
	  rst = 1;
	  #20;
	  
	 end

endmodule